import React from "react";
import Header from "../components/Header";

const CartPage = () => {
  return (
    <div>
      <Header />
      <p>Cart Page</p>
    </div>
  );
};

export default React.memo(CartPage);
